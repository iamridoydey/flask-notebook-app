apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook-api.fullname" . }}
  labels:
    {{- include "notebook-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notebook-api.selectorLabels" . | nindent 4 }}
