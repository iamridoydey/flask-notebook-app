apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook-db.fullname" . }}
  labels:
    {{- include "notebook-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notebook-db.selectorLabels" . | nindent 4 }}
