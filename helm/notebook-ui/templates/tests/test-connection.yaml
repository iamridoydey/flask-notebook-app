apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notebook-ui.fullname" . }}-test-connection"
  labels:
    {{- include "notebook-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "notebook-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
