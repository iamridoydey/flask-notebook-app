apiVersion: v1
kind: Service
metadata:
  name: {{ include "notebook-ui.fullname" . }}
  labels:
    {{- include "notebook-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "notebook-ui.selectorLabels" . | nindent 4 }}
